local StatsLinks = {};
local StatsClass = {};
local StatsMt = {};

return function(Interface)
  assert(Interface, "Something is wrong: No Interface for Stats", 2);
  local container = {
    Attributes = {

    };
    Mods = {

    };
    BaseMods = {

    };
    Interface = Interface;
  }
end;
